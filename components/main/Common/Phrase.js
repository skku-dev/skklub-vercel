import styled from "@emotion/styled";

const Phrase = styled.div`
  font-size: 2rem;
  font-weight: 500;

  @media (max-width: 425px) {
    font-weight: 500;
    font-size: 1rem;
  }
`;

export default Phrase;
